# AltegioBookingsParser
To start, just clone the repo and run app.py with python and set parameters with the built-in GUI.
I'll write a more detailed instructions as well as make it automatically check for dependencies later.
